import { BoidWorldOptions } from "../objects/BoidWorld"

export const DEFAULT_BOID_WORLD: BoidWorldOptions = {
    initialBoids: 100,
    timescale: 0.1,
    width: screen.width,
    height: screen.height,
}
